<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <title>تعديل فاتورة</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-100 text-slate-800">
  <div class="max-w-xl mx-auto p-4 mt-6 bg-white rounded-xl shadow">
    <h1 class="text-xl font-bold mb-4">تعديل فاتورة</h1>

    <form action="/receipts/<%= receipt._id %>/edit" method="POST" class="space-y-3">
      <div>
        <label class="text-xs text-slate-500">التاريخ (نصي)</label>
        <input name="date_raw" value="<%= receipt.date_raw || '' %>" class="w-full border rounded px-2 py-1 text-sm" />
      </div>

      <div>
        <label class="text-xs text-slate-500">البائع</label>
        <input name="seller" value="<%= receipt.seller || '' %>" class="w-full border rounded px-2 py-1 text-sm" />
      </div>

      <div>
        <label class="text-xs text-slate-500">الإجمالي (grand_total)</label>
        <input name="grand_total" type="number" value="<%= receipt.grand_total || 0 %>" class="w-full border rounded px-2 py-1 text-sm" />
      </div>

      <div>
        <label class="text-xs text-slate-500">الخصم (discount_total)</label>
        <input name="discount_total" type="number" value="<%= receipt.discount_total || 0 %>" class="w-full border rounded px-2 py-1 text-sm" />
      </div>

      <div>
        <label class="text-xs text-slate-500">طريقة الدفع</label>
        <input name="payment_method" value="<%= receipt.payment_method || '' %>" class="w-full border rounded px-2 py-1 text-sm" />
      </div>

      <div>
        <label class="text-xs text-slate-500">المبلغ المدفوع</label>
        <input name="amount_paid" type="number" value="<%= receipt.amount_paid || 0 %>" class="w-full border rounded px-2 py-1 text-sm" />
      </div>

      <div>
        <label class="text-xs text-slate-500">الباقي (change_amount)</label>
        <input name="change_amount" type="number" value="<%= receipt.change_amount || 0 %>" class="w-full border rounded px-2 py-1 text-sm" />
      </div>

      <div class="flex justify-between mt-4">
        <a href="/receipts" class="px-3 py-2 text-sm rounded bg-slate-200 hover:bg-slate-300">رجوع</a>
        <button type="submit" class="px-3 py-2 text-sm rounded bg-emerald-600 text-white hover:bg-emerald-700">
          حفظ التعديل
        </button>
      </div>
    </form>
  </div>
</body>
</html>
